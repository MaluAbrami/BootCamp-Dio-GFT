let preco = 10;
preco += 4; //Mesma coisa que 'preco = preco + 4'
console.log(preco);

preco -= 4; //Mesma coisa que 'preco = preco - 4'
console.log(preco);

preco /= 5; //Mesma coisa que 'preco = preco / 3'
console.log(preco);

preco *= 3; //Mesma coisa que 'preco = preco * 3'
console.log(preco);